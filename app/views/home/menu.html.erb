<br>
<h1>Menu</h1>
<br>	
<ul class="nav nav-pills">
	<% @section_list.each do |a| %>
		<li class="nav-item">
			<%= link_to a.name, menu_path(section: a.name), class: "nav-link #{'active' if params[:section] == a.name}" %>
		</li>
	<% end %>
</ul>
'<% section.food_items'